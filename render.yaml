services:
  - type: web
    name: q-sat-ai
    env: python
    plan: free
    buildCommand: |
      echo "Python version:"
      python --version
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.14
